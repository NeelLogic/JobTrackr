name: Continuous Integration

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend tests and build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: Backend/jobtrackr

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: Backend/jobtrackr/pom.xml

      - name: Run backend tests and package application
        run: ./mvnw --batch-mode --no-transfer-progress verify

  frontend:
    name: Frontend checks, tests, and build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: Frontend

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: Frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Check frontend formatting
        run: npx prettier --check "src/**/*.{ts,html,scss}" "public/**/*.js" "scripts/**/*.mjs" "angular.json" "package.json" "tsconfig*.json" "../compose.yaml" "../render.yaml" "../.github/workflows/*.yml"

      - name: Validate runtime configuration
        run: npm run config:check

      - name: Run frontend tests
        run: npm test -- --watch=false

      - name: Build frontend for production
        run: npm run build

  containers:
    name: Container configuration and builds
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate Docker Compose configuration
        run: docker compose --file compose.yaml config --quiet

      - name: Build backend image
        run: docker build --file Backend/jobtrackr/Dockerfile --tag jobtrackr-backend:ci Backend/jobtrackr

      - name: Build frontend image
        run: docker build --file Frontend/Dockerfile --tag jobtrackr-frontend:ci Frontend

  dependency-review:
    name: Dependency security review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Review dependency changes
        uses: actions/dependency-review-action@v4
